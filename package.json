{
  "name": "@madappgang/update-by-path",
  "version": "1.1.19",
  "description": "Easy deep immutable updates to objects",
  "main": "dist/index.js",
  "scripts": {
    "build": "rm -rf dist && babel src -d dist",
    "build:w": "rm -rf dist && babel --watch src -d dist",
    "test": "jest",
    "test:w": "jest --watch",
    "test:cov": "jest --coverage"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/MadAppGang/update_object_by_path.git"
  },
  "keywords": [
    "update",
    "object",
    "path",
    "immutable",
    "recursive",
    "deep",
    "insert",
    "upsert"
  ],
  "author": "Denys Provodnikov [MadAppGang]",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/MadAppGang/update_object_by_path/issues"
  },
  "homepage": "https://github.com/MadAppGang/update_by_path",
  "devDependencies": {
    "@babel/cli": "^7.4.4",
    "@babel/preset-env": "^7.4.5",
    "jest": "^24.8.0"
  },
  "dependencies": {}
}
